services:
  app:
    image: ghcr.io/proapi-hub/prolab:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prolab
    ports:
      - "${PROLAB_PUBLISH:-3000:3000}"
    restart: unless-stopped
